import * as React from "react";
import { View, Text } from "react-native";

export default function SettingScreen({ navigator }) {
    return (
        <View>
            <Text>This is Setting screen</Text>
        </View>
    );
}