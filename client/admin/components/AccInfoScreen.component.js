import * as React from "react";
import { View, Text } from "react-native";

export default function AccInfoScreen({ navigator }) {
    return (
        <View>
            <Text>This is AccInfo screen</Text>
        </View>
    );
}